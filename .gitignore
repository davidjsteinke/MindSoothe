# Sensitive content directory — see Project Frame > Sensitive Content Disclaimer
sensitive/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Build artifacts
dist/
build/
*.egg-info/

# Dev-build staging (deploy.sh dev stamps the Mind Dev twin here; local-only)
.build/

# BigWigs packager: local clone + its build output (both local-only, never committed)
packager/
.release/

# Editor / tooling backups
*.bak.*

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Test corpus working files (corpus itself IS tracked; working files aren't)
.test-corpus-cache/

# Claude Code per-project state (session history, not source)
.claude/
